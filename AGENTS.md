# Agent instructions for sergn.io

## GitHub account
This is a personal project, owned by the `sergnio` GitHub account (origin remote
uses the `github.com-sergnio` SSH host alias). Before committing or pushing,
run `github-account personal` to switch the active `gh` account, pin the local
git identity, and select the matching SSH host. Verify with `github-account status`.
